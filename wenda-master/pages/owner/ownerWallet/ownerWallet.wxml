
<view class="bannerContainer">
    <image class="topBanner" src="../../../images/redBanner.png"/>
    <view class="out-wrapper">
        <image class="headImg" src="{{userInfo.avatarUrl}}"/>
    </view>
    <view class="nickName">{{userInfo.nickName}}</view>
    <view class="pocket-money">￥{{amountFloat}}</view>
    <!--<view class="income-money">共收入{{amountInt}}元（满1元自动转入微信钱包）</view>-->
    <view class="income-money">满1元后将会在17:00-17:30自动转入微信钱包</view>
</view>
    
<block wx:for="{{list}}" wx:for-item="item" wx:key="{{index}}">
    <view class="content-wrapper" wx:if="{{item.income == 1}}">
        <image class="pocket-icon" src="../../../images/income.png"></image>
        <view class="from">红包-来自<text>{{item.fromUserName}}</text></view>
        <view class="time">{{item.createTime}}</view>
        <view class="income">+ {{item.amount}} <text>元</text></view>
        <view class="type" wx:if="{{item.comment}}">{{item.comment}}</view>
    </view>
    <view class="content-wrapper" wx:elif="{{item.income == 0}}">
        <image class="pocket-icon" src="../../../images/expend.png"></image>
        <view class="from">自动提现-<text>微信钱包</text></view>
        <view class="time">{{item.createTime}}</view>
        <view class="income_f">- {{item.amount}} <text>元</text></view>
    </view>
</block>
<view style="text-align:center" wx:if="{{empty}}">
    <image class="noContents" src="{{dataEmpty}}"></image>
    <view class="noText"><text>虽然没有赚到什么钱,但能够帮助他人,我也是开心的...</text></view>
</view>
