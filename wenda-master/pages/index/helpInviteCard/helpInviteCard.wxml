
<!--<view wx:if="{{loading}}" class="loading">
    <image src="../../../images/votef1.png"></image>
    <text>正在生成专属邀请卡,请稍等一会儿~</text>
</view>

<view wx:else bindtap="previewPic">
    <image class="inviteCard" src="{{imgSrc}}" mode="widthFix"></image>
</view>-->
 <image class="inviteCard" src="{{imgSrc}}" mode="widthFix" bindtap="previewPic"></image>
