/* 成绩卡片 */
.card-score {
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 30rpx;
  background-color: #fff;
  box-shadow: 0 0 6rpx 6rpx #eee;
  border-radius: 4rpx;
  overflow: hidden;
}
.cj-text{
    display: flex;
    flex-flow: column nowrap;
    justify-content: flex-start;
    padding-left: 25rpx;
}
.cj-header-title{
    font-size: 20pt;
    font-weight: bold;
    line-height: 150%;
    margin-bottom: 15rpx;
    color: #222;
}
.cj-header-info{
    font-size: 10pt;
    line-height: 160%; 
    color: #888;
}
.card-score__head {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-bottom: 30rpx;
  border-bottom: solid 1rpx #eee;
}
.card-score__title {
  margin-bottom: 5rpx;
  font-size: 12pt;
  font-weight: bold;
  color: #000;
}
.card-score__user-info {
  margin-top: 15rpx;
  font-size: 12pt;
  color: #999;
}
.card-score__box {
  min-height: 520rpx;
}
.card-score__item {
  box-sizing: border-box;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin: 10rpx 0;
  padding: 30rpx 0;
  border-bottom: solid 1rpx #eee;
}
.card-score__course-name,
.card-score__mark {
  font-size: 11pt;
  font-weight: 500;
  color: #333;
}
.card-score__mark {
  margin-left: 45rpx;
}
.card-score__share {
  display: flex;
  align-items: center;
  margin: 0;
  padding: 0;
  border: none;
  outline: none;
  background-color: transparent;
}

/* reset button */
button::after {
  content: '';
  width: 0;
  height: 0;
}
.card-score__ic-share {
  width: 45rpx;
  height: 45rpx;
}
