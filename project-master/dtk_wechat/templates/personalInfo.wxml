<template name="personal">
  <view class="listbox clearfix fillet">
    <view class="list-title-avatar clearfix">
      <image class="avatarsrc" src="{{msgList.creator.avatar}}" data-src="{{msgList.creator.avatar}}"  bindtap="previewImage"></image>
    </view>
    <view class="listbox-info">
      <view class="listbox-info-li clearfix">
        <image src="{{msgList.creator.genderimage}}"></image>
        <view class="temp-width">
          <text class="creatorname">{{msgList.creator.name}}</text>
          <text class="user-id">{{msgList.creator.user_id}}</text>
        </view>
      </view>
      <view class="listbox-info-li clearfix"><image src="{{msgList.creator.addressimage}}"></image>
        <view class="temp-width">
          <text>{{msgList.creator.address}}</text>
        </view>
      </view>
      <view class="listbox-info-li clearfix">
        <image src="{{msgList.creator.companyimage}}"></image>
        <view class="temp-width">
          <text class="companyname">{{msgList.creator.company}}</text>
        </view>
      </view>
    </view>
  </view>
</template>
<template name="nothing">
  <view class="nothing">
    <image class="nothing-image" src="../../image/nothing.png"/>
    <view class="nothing-text">目前没有相关记录~</view>
  </view>
</template>
<template name="nobind">
  <view class="nothing" style='margin:200rpx 0 100rpx;'>
    <image class="nothing-image" src="../../image/nothing.png"/>
    <view class="nothing-text">小贴士：当您成功绑定手机，并选择【公开】信息时，即可开启票友通讯录功能。~</view>
  </view>
</template>