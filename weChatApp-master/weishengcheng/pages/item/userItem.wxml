<template name="user-item">
    <view class="user" bindtap="selectUser" data-name="{{item.name}}" data-avatar="{{item.avatar}}">
        <image lazyload="true" src="{{item.avatar}}?imageView2/1/w/180/h/180" mode="aspectFill"></image>
        <text>{{item.name}}</text>
    </view>
</template>