<import src="../../../common/head.wxml" />
<import src="../../../common/foot.wxml" />

<view class="container">
  <!-- <template is="head" data="{{title: '六色拼盘'}}"/> -->
  <view class="page-body">

    <view class="page-section page-section-gap">
      <view class="page-section-ctn">
        <image class="image" src="{{pic}}" />
        <view>第 {{tnum}} 次, 成功 {{snum}} 次</view>
      </view>

      <view class="btn-area" id="buttonContainer">
        <view class="button-sp-area">
          <button type="primary" plain="true" bindtap="sample">小试手气</button>
        </view>
      </view>

      <view class="index-desc">.</view>
      <view class="btn-area" id="buttonContainer">
        <view class="button-sp-area">
          <button type="primary" plain="true" bindtap="restart">再来一次</button>
        </view>
      </view>

      <view class="page-section page-section-gap">
        <view class="page-section-ctn">
          <image class="image" src="../../resources/pic/app_k.jpg" />
          <!-- <image class="image" src="../../resources/pic/mmqrcode.png"/>  -->
        </view>
      </view>

    </view>
  </view>
  <template is="foot" />
</view>