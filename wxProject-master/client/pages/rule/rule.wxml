<view wx:if="{{isshow}}" class="container">
  
   <view class="item" wx:for="{{ruleInfo}}">
      <label class="ranknum">{{index+1}}</label><label class="username">{{item.name}}</label><label class="dw">:{{item.start}}-{{item.end}}</label>
   </view>
  <view class="footer">
    <view class="footeritem footerfirst" bindtap="gotoIndex">首页</view>
    <view class="footeritem" bindtap="gotoRank">榜单</view>
     <view class="footeritem">风云榜</view>
    <view class="footeritem">规则</view>
  </view>
</view>