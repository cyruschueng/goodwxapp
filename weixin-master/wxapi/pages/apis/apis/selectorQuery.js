/**
 * API -- WXML SelectorQuery 对象
 * 
 * >= 1.6.0
 * 
 * 由 wx.createSelectorQuery 接口创建返回得到
 * 
 * 可在这个对象实例上使用 select 等方法节点
 * 
 */

module.exports = {
  in(component) {

  },

  select(selector) {

  },

  selectAll(selector) {

  },

  selectViewport() {

  },

  exec([callback]) {

  },

  nodesRef: {
    boundingClientRect([callback]) {},

    scrollOffset([callback]) {},

    fields(fields, [callback]) {}
  }
}
