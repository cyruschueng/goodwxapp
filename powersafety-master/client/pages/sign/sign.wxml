<view class='title'>
  <text> 选择好友之间较量的领域</text>
</view>
<view>
  <button wx:key="index" wx:for="{{question_sort}}" class="{{index+1==sortId&&sortId!=='' ? 'animated fadeIn' : ''}} sort" bindtap='beginSign' data-sortid='{{item.id}}' data-sortname='{{item.sort}}'>{{item.sort}}</button>
</view>