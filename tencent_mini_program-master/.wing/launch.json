{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Debug Weixin App",
            "type": "weapp",
            "request": "launch",
            "project": "${workspaceRoot}"
        }
    ]
}