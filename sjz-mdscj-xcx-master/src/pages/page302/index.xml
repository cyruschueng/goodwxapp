<form bindsubmit="formSubmit" report-submit='true'>
  <button formType="submit" class="formIdBtn">
    <view class="webview-wrap">
      <web-view  wx:if="{{state.url != null}}" src="{{state.url}}"></web-view>
    </view>
  </button>
</form>
