import * as types from '../../types';
import { call, put, take, fork } from 'redux-saga/effects';

export function *changeTabIndex () {
  while (true) {
    // const payload = yield take(types.ORDER_TYPE_CHANGE_REQUEST);
    // yield
  }
}

export default function * () {
  yield [
    // (changeTabIndex)
  ];
}
