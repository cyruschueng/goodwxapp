<view class="sku-item">
  <text class="sku-item-title">{{props.sku.specName}}</text>
  <view class="sku-item-btns">
    <view class="sku-item-btn" wx:for="{{props.skus}}" wx:key="{{item.id}}">
      {{item.name}}
    </view>
  </view>
</view>
