<!-- 侧边栏 -->
<c-menu 
    is-full="{{isFull}}"
    is-show-menu="{{ isShowMenu }}" 
    menu-money="{{ menuMoney }}" 
    icon="{{icon}}" 
    nick-name="{{nickName}}"
    ></c-menu>


<view class="end-footer">
    <view class="footer-btn left-btn" bindtap="toIndex">首页</view>
    <view class="footer-btn right-btn">我的</view>
</view>