<view class="video">
    <video id="myVideo" class="myVideo" src="{{src}}" binderror="playError"></video>
    <c-msg cmsg-is-show="{{!cmsgIsShow}}" cmsg-value="{{cmsgValue}}"></c-msg>
    <c-resouce message="{{ message }}" icon="{{ icon }}" nick-name="{{ nickName }}"></c-resouce>
</view>