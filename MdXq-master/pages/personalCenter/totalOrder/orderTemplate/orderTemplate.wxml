<template name='obligationCenter'>
  <view class='obligationCenter' >
    <view class='obligationCenterLeft'>
      <image src='{{mallProduct.exhibition}}'></image>
    </view>
    <view class='obligationCenterMiddle'>
      <view>{{mallProduct.product_name}}</view>
    </view>
    <view class='obligationCenterRight'>
      <view>￥{{amount}}</view>
      <view wx:if='{{mallProduct.quantity!=null}}'>X{{mallProduct.quantity}}</view>
      <view wx:else>X1</view>
    </view>
  </view>
</template>