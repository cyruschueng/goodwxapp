<view class="item">
    <view class="con" wx:for="{{tags}}" wx:if="{{item != ''}}">
        <view >{{item}}</view>
    </view>
</view>
<view  class="soli" wx:if="{{tags.length == 0}}">
  <view class="title">您还没获得标签哦！！！</view>
</view>