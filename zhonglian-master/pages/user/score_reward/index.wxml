<view class="">
    <checkbox-group>
        <label class="item flex gridXt" wx:for='{{data}}' wx:key='{{item.ORDER}}' catchtap='onNavigateTap' data-url='person?dept_id={{item.dept_id}}'>
            <view class="sub ml10">{{item.dept_name}}</view>
        </label>
    </checkbox-group>
</view>