<block wx:if="{{data.length}}">
    <view class="card" wx:for="{{data}}" wx:key="{{item.id}}">
        <view class="card-header">
            <view class='fr'>
                <text class='blue' wx:if="{{item.manager_id>0}}">{{item.manager_name}} 已审核</text>
            </view>
            <view class='card-header-title'>{{item.score}}</view>
        </view>
        <view class="card-content">
            <view class='card-content-inner'>
                <view>{{item.remark}}</view>
                <view class='imgs' catchtap='onPreviewTap' data-imgs="{{item.attachments}}">
                    <view class='imgs-item ' wx:for='{{item.attachments}}' wx:key='{{index}}'>
                        <image src='{{hostUrl}}{{item}}' data-index='{{index}}' />
                    </view>
                </view>
            </view>
        </view>
        <view class="card-footer">
            <view class="footer-item text-grey">{{item.create_time}}</view>
            <view class="footer-item tr">
                <text class="text-default" wx:if="{{item.status==0}}">等待审核</text>
                <text class="text-fail" wx:elif="{{item.status==2}}">审核不通过</text>
                <text class="text-success" wx:else>审核通过</text>
            </view>
        </view>
    </view>
</block>
<view class="no-data" wx:else>
    <view class="no-data-inner">
        <view class="no-data-msg">亲，你还没有任何申请记录~</view>
    </view>
</view>

<view class="float-btn-group">
    <view class="btn" catchtap="onNavigateTap" data-url="apply">
        <text class="iconfont icon-shenqing1"></text>
    </view>
</view>