<import src="/pages/Template/Template.wxml" />
<include src='/utils/wxml/common' />
<view wx:if="{{nonet}}">
  <block wx:for="{{messageList}}" wx:key="{{index}}">
    <template is="identityList" data="{{item:item}}"></template>
  </block>
</view>