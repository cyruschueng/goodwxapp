<view class="list_box">

  <view class="li">
    <text>联系人：</text>
    <input bindinput="inputusername" class="more" placeholder="输入收货联系人姓名" value="{{username}}" />
  </view>

  <view class="li">
    <text>联系电话：</text>
    <input bindinput="inputphone" class="more" placeholder="输入收货联系电话" value="{{phone}}" />
  </view>

   <!-- <view class="li">
    <text>地址：</text>
    <picker mode="region" bindchange="bindRegionChange" value="{{region}}">
      <view class="picker">
        {{region[0]}}，{{region[1]}}，{{region[2]}}
      </view>
    </picker>
  </view>  -->

  <view class="li" wx:if="{{1}}" catchtap="location">
    <text>所在地区：</text>
    <view>{{chooseLocation.address}}</view>
  </view> 

  <view class="li">
    <text>详细地址：</text>
    <input bindinput="inputaddress" class="more" placeholder="输入街道、楼牌号" value="{{addrDetail}}" />
  </view>

   

  <!-- <view class='li' wx:if="{{!Default}}">
    <text>是否默认：</text>
    <checkbox-group bindchange="checkboxChange">
      <label class="checkbox" wx:for="{{items}}" wx:key="{{index}}">
        <checkbox value="{{item.name}}"  checked="{{item.checked}}" />{{item.value}}
      </label>
    </checkbox-group>
  </view> -->

</view>





<view wx:if="{{!id}}" bindtap="save" class="btn_bom">
  <view class="classname">保存</view>
</view>

<view wx:if="{{id}}" bindtap="edit" class="btn_bom">
  <view class="classname">保存</view>
</view>