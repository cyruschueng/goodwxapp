<view class='queue_title'>{{deskNo}}号桌就餐人数</view>

<view report-submit class="queue" bindsubmit="queue_sub">

  <view class='scroll_view' >

    <view data-index='{{index}}' wx:key="{{index}}" wx:if="{{index != 0}}" catchtap='change_active_index' class='li {{active_index === index ? "active" : ""}}' wx:for="{{21}}"><view>{{index}}</view></view>

  </view>


</view>


  <view bindtap='takeoutpay' class='scan'>立即点餐</view>
