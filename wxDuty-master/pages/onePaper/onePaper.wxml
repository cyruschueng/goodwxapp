<!--pages/onePaper/onePaper.wxml-->
<import src="paperItem/paperItemTemplate.wxml" />
<view>
  <block wx:for="{{posts_key}}" wx:key="{{index}}">
    <view catchtap="onPostTap" data-postId="{{item.postId}}">
      <template is="postItem" data="{{...item}}" />
    </view>
  </block>
</view>