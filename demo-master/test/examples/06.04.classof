function classof(o) {
    if (o === null) return "Null";
    if (o === undefined) return "Undefined";
    return Object.prototype.toString.call(o).slice(8,-1);
}



call 和 apply 调用函数

object.tostring(),//默认，对象从原型链中继承
object.prototype.tostring(),//对象从原型链中继承
object.prototype.tostring.call(o)//每个对象都能通过 Object.prototype.toString() 来检测




