<!--  自定义toast组件  -->
<view class='dialog-mask' wx:if="{{show == true}}" bindtap='close'>
   <view class='dialog-content'>
    <view class='dialog-title'>{{title}}</view>
    <slot></slot> 
  </view>     
</view>