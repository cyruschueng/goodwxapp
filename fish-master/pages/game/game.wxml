<!--index.wxml-->
<view class="game">
  <view class="game-main">
      <view class="river">
          <view class="wave"></view>
          <view class="wave"></view>
          <view class="wave"></view>
          <view class="wave"></view>
        </view>
        <image
          src="{{seaHourseImg}}"
          class="sea-hourse"
          bind:touchstart="touchStart"
          bind:touchend="touchEnd"
          >
        </image>
        <view class="arrow-tip">↓</view>
        <view class="dot {{dotMove}}"></view>
        <view class="counts">{{counts}}</view>
        <audio poster="{{poster}}" name="{{name}}" author="{{author}}" src="{{src}}" id="myAudio" controls="{{controls}}" loop></audio>
  </view>
</view>