<!--pages/find/find.wxml-->
<view class="container">
	<view class="birthday-password" wx:for="{{birthdayList}}" bindtap='findInform' data-index="{{index}}">
		<view class="title">	{{item.title}}</view>
		<image class="content-image" mode='widthFix' src="{{item.img}}"></image>
	</view>
</view>
