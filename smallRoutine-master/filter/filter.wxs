var listFilter = {
    removeNewLine: function(con){
      var info = con;
      if (con.indexOf("\n") != -1) {
        info = con.split("\n").join("");
      }
      return info;
    }
}
module.exports = {
  removeNewLine: listFilter.removeNewLine
}