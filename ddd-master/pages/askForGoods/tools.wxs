//用 let 会报错

var isActiveShare = function(answer,isOver){
  if (answer.ga_progress == answer.ga_max || answer.share == 0 || isOver){
    return false
  }else{
    return true
  }
}
module.exports.isActiveShare = isActiveShare;

//module.exports.msg = "some msg";
