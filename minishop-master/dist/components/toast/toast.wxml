<view wx:if="{{toast.show}}" class="m-toast-container uf uf-center" style="{{toast.mask ? 'background: rgba(255, 255, 255, 0)' : ''}}">
  <view class="m-toast uf uf-center">{{toast.title}}</view>
</view>
