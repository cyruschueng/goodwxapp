
<import src="../template/template.wxml" />
<view hidden="{{ishistory}}">
    <view class="clean-history">
        <text bindtap="cleanHistory">清除全部</text>
    </view>

    <view wx:for="{{watchRecordData}}" wx:key="{{item}}"  wx:for-index="idx"  class="history-item">
        <text class="history-date">{{idx}}</text>
        <view class="history-content" wx:for="{{item}}" wx:key="{{dateitem.id}}" wx:for-item="dateitem">
            <navigator url="../video/video?video_id={{dateitem.id}}">
            <image class="item-image" src="{{dateitem.thumb}}"></image>
            <text class="item-text">{{dateitem.title}}</text>
            </navigator>
        </view>

    </view>
</view>

<view class="nohistory" hidden="{{!ishistory}}">
    <template is="empty" data="{{...empty}}" />
</view>


