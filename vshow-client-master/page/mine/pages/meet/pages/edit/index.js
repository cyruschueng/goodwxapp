const { imgDirUrl } = require('../../../../../../config');
const { NetRequest, showTips } = require('../../../../../../utils/util.js');
Page({
  data: {
    iconAddUrl: `${imgDirUrl}icon_add.png`,
  }
})